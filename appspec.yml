version: 0.0
os: linux

files:
  - source:  /
    destination: /home/ubuntu/DGIT_V2_Client/build/

permissions:
  - object: /home/ubuntu/DGIT_V2_Client
    mode: 755
    pattern: "**"
    owner: ubuntu
    group: ubuntu

hooks:
  AfterInstall:
    - location: deploy.sh
      timeout: 60
      runas: ubuntu