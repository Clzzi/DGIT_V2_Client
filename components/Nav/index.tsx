import * as S from './styles';
import LOGO from 'assets/Logo.svg';

const Nav = (): JSX.Element => {
  return (
    <S.Header>
      <S.HeaderContent>
        <LOGO />
        <S.HeaderButton>
          
        </S.HeaderButton>
      </S.HeaderContent>
    </S.Header>
  );
};

export default Nav;
